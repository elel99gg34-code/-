# EggMate (에그메이트)

**Steal An Egg (계란을 훔치세요)** — 로블록스 동접 1위 게임용 데스크톱 계산기.

무게·뮤테이션 수입 공식, 부화 확률, 바이옴 진입 판정, 업그레이드 회수(ROI), 퓨즈 손익을
전부 계산해 주는 Windows 앱입니다.

<br>

## 이 앱이 하지 않는 것 — 먼저 읽어 주세요

이 앱은 **로블록스 클라이언트와 전혀 상호작용하지 않습니다.** 다음 기능은 없고, 앞으로도
넣지 않습니다.

| 안 넣는 것 | 이유 |
|---|---|
| 투시(ESP) · 월핵 · 벽 너머 알/플레이어 표시 | 클라이언트 메모리 읽기나 DLL 인젝션이 필수 → 이용약관 위반, 영구정지 사유 |
| 게임 프로세스 메모리 읽기/쓰기, 인젝션 | 위와 같음 |
| 오토클릭 · 매크로 · 오토파밍 | 조작 자동화 금지 |
| 패킷 가로채기/조작 | 서버 통신 조작 금지 |
| 계정 정보 수집, 외부 전송 | 수집하지 않음 |

특히 이 게임은 **다른 플레이어의 알을 뺏는 구조**라, 투시 같은 기능은 실제 상대 플레이어에게
피해를 줍니다. "합법 투시"라는 건 존재하지 않습니다.

그리고 **"다음에 나올 레어 알"은 예측할 수 없습니다.** 부화 결과는 서버에서 굴리는 난수이고
각 시행은 서로 독립이라, 클라이언트 쪽에서 미리 알 방법이 원천적으로 없습니다. 대신 이 앱은
확률을 정확히 계산해 주고, 내가 직접 기록한 결과의 통계를 내 줍니다. 그게 실제로 도움이 되는
버전입니다.

**요약: 이 앱은 계산기이자 메모장입니다. 게임을 대신 플레이해 주지 않습니다.**

<br>

## 기능 40가지

### A. 수입 · 최적화

| # | 기능 | 하는 일 |
|---|---|---|
| 1 | **펜 빌더** | 슬롯 제한 아래 수입이 최대가 되는 조합을 계산. 펫 하나가 슬롯 하나를 쓰고 수입이 독립이라 '정렬 후 자르기'가 **수학적으로 정확한 최적해** (테스트에서 전수 조합과 대조) |
| 2 | 인벤토리 연동 장착 | 계산된 최적 조합을 인벤토리 장착 상태로 한 번에 저장 |
| 3 | 슬롯 가치 곡선 | 슬롯을 1개씩 늘릴 때 각각 얼마를 더 벌어 주는지 (체감 수익 확인) |
| 4 | 교체 시뮬레이션 | A를 빼고 B를 넣으면 수입이 어떻게 변하는지 |
| 5 | 업그레이드 구매 순서 | 회수 빠른 순으로 정렬하고, 복리를 반영한 구매 일정을 계산 |
| 6 | 재투자 성장 곡선 | 번 돈을 얼마나 다시 넣느냐에 따른 수입 성장 비교 |
| 7 | 스피드 훈련 시간 | 러닝머신 획득량 × 트레일 배율 → 목표 스피드까지 걸리는 시간 |
| 8 | 런 효율 | 알 개수 × 성공률 × 가치 ÷ 런 시간 → 시간당 기대 수익 |

### B. 확률 · 시뮬레이션

| # | 기능 | 하는 일 |
|---|---|---|
| 9 | **몬테카를로 시뮬레이터** | 가상 플레이어 수만 명을 돌려 시도 횟수 분포를 보여 줌 (평균이 아니라 **실제로 겪는 폭**) |
| 10 | 운빨 격차 지표 | 상위 10% ↔ 하위 10% 배율. 0.01% 확률이면 약 **22배** 차이 |
| 11 | 세션 시뮬레이션 | 확률표를 놓고 N번 까 봤을 때 등급별로 몇 개 나오는지 |
| 12 | 도감 완성 기대 시도 | 쿠폰 수집가 문제를 수치적분으로 계산 (균등 확률 닫힌 형태와 일치 검증) |
| 13 | 다중 목표 | 여러 확률을 동시에 — '아무거나 하나' / '전부 모으기' |
| 14 | 재현 가능한 시드 | 같은 시드면 항상 같은 결과. 남에게 보여 줄 때 유용 |
| 15 | Wilson 신뢰구간 | 내 실측 확률의 95% 구간. 표본이 적으면 구간이 넓게 나와 '아직 판단 이르다'가 보임 |

### C. 인벤토리 · 컬렉션

| # | 기능 | 하는 일 |
|---|---|---|
| 16 | **인벤토리** | 가진 펫을 뮤테이션·무게·수량·장착 상태까지 관리 |
| 17 | 실수입 자동 계산 | 1마리 $/s 와 수량 합계 $/s 를 무게 공식으로 즉시 계산 |
| 18 | 퓨즈 후보 자동 탐지 | 같은 펫 3마리 이상을 찾아냄. **뮤테이션 붙은 펫은 손해라 제외** |
| 19 | 도감 완성도 | 전체 / 바이옴별 / 등급별 진행률 |
| 20 | 없는 펫 목록 | 아직 못 얻은 펫을 바이옴으로 걸러서 표시 |
| 21 | 위시리스트 | 노리는 펫을 우선순위와 함께 (대시보드에 표시) |
| 22 | CSV 내보내기 / 가져오기 | 인벤토리와 부화 기록 모두 |

### D. 기록 · 분석

| # | 기능 | 하는 일 |
|---|---|---|
| 23 | **다중 프로필** | 부캐별로 기록을 완전히 분리. 생성 · 이름 변경 · 삭제 |
| 24 | 성장 타임라인 | 스피드 · 자산 · 수입을 기록하고 막대 그래프로 추이 확인 |
| 25 | 부화 활동 그래프 | 최근 14일 일별 부화 횟수 |
| 26 | 백업 / 복원 | DB + 설정 + 게임 데이터를 ZIP 한 벌로 |
| 27 | 대시보드 | 프로필 · 총수입 · 도감 · 보유 · 부화 · 스피드를 카드 6개로 |
| 28 | 자동 마이그레이션 | 구버전 `hatches.db` 를 새 DB 로 한 번만 옮겨 옴 |

### E. 편의 · UI

| # | 기능 | 단축키 |
|---|---|---|
| 29 | **사이드바 네비게이션** (14개 화면) | `Ctrl+1`~`Ctrl+9` |
| 30 | 라이트 / 다크 테마 | `Ctrl+T` |
| 31 | 글자 크기 조절 | `Ctrl++` / `Ctrl+-` |
| 32 | 창을 항상 맨 위로 | `Ctrl+P` |
| 33 | **컴팩트 오버레이** (게임하면서 옆에) | `Ctrl+M` |
| 34 | **전역 검색** (펫 · 화면) | `Ctrl+K` |
| 35 | 즐겨찾기 ★ | 도감에서 토글 |
| 36 | 결과 클립보드 복사 | 각 화면의 복사 버튼 |
| 37 | 최근 계산 기록 | 설정에서 확인 |
| 38 | 창 위치 · 크기 · 마지막 화면 기억 | 자동 |
| 39 | 단축키 도움말 | `F1` |
| 40 | 실시간 상태바 | 프로필 · 도감 · 보유 · 부화 현황 |

<br>

## 화면 14개

대시보드 · 펫 도감 · 도감 완성도 · 인벤토리 · 펜 빌더 · 수입 계산기 · 확률 계산기 ·
시뮬레이터 · 퓨즈 판단 · 부화 기록 · 진행 플래너 · 성장 기록 · 설정 · 정보

<br>

## 핵심 계산 — 무게 기반 수입 공식

게임이 실제로 쓰는 공식을 그대로 구현했습니다.

```
r = 실제 무게 / 기본 무게

r  < 125 :  수입 = 기본수입 × r^(37/60)                × 뮤테이션배율
r >= 125 :  수입 = 기본수입 × 125^(13/60) × r^(2/5)     × 뮤테이션배율
```

두 식은 `r = 125` 에서 정확히 이어집니다 (`125^(13/60) × 125^(24/60) = 125^(37/60)`).
공개된 검증값(100배 → 약 17배, 200배 → 약 23배, 1000배 → 약 45배)과 일치하며,
이 검증은 테스트로 고정되어 있습니다.

<br>

## 설치

### 설치 프로그램 (권장)
[Releases](../../releases) 에서 `EggMate-x.y.z-Setup.exe` 를 받아 실행하세요.
관리자 권한 없이 설치됩니다. 설치 파일은 약 28MB 입니다.

릴리스가 아직 없다면 [Actions](../../actions/workflows/build-windows.yml) 탭에서
최신 성공 실행을 열고 Artifacts 의 `EggMate-x.y.z-Setup` 을 받으면 됩니다.

### 무설치(포터블)
같은 위치의 `EggMate-x.y.z-portable` 아티팩트를 풀고 `EggMate.exe` 를 실행하세요.

### 소스로 실행
```bash
pip install -r requirements.txt
python -m eggmate            # PYTHONPATH=src 필요
```

<br>

## 직접 빌드하기 (Windows)

```powershell
pip install -r requirements-dev.txt pyinstaller==6.11.1
python packaging\make_icon.py src\eggmate\data\icon.ico
python -m pytest tests\ -q
python packaging\smoke_test.py
pyinstaller --noconfirm --clean packaging\eggmate.spec
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /DAppVersion=1.0.0 packaging\installer.iss
```

결과물은 `dist\EggMate-1.0.0-Setup.exe` 입니다.

CI(`.github/workflows/build-windows.yml`)가 푸시마다 같은 과정을 자동으로 돌리고,
`v*` 태그를 붙이면 릴리스에 설치 파일을 첨부합니다.

<br>

## 데이터가 오래되면

게임은 자주 패치됩니다. **인게임 Index 와 업그레이드 보드가 항상 최종 기준입니다.**

값이 다르면 앱에서 직접 고칠 수 있습니다:

1. `정보` 탭 → `데이터 파일 편집용 복사본 만들기`
2. 만들어진 `%APPDATA%\EggMate\gamedata.json` 을 메모장으로 수정
3. `다시 불러오기` (또는 `Ctrl+R`)

JSON 이 깨져 있으면 앱이 조용히 기본 데이터로 되돌아가므로 앱이 못 켜지는 일은 없습니다.

<br>

## 데이터 출처

수치는 커뮤니티 위키/가이드를 교차 검증해 모았습니다. 전체 목록은 `정보` 탭이나
`src/eggmate/data/gamedata.json` 의 `sources` 필드에 있습니다.

알려진 불일치는 데이터 파일에 그대로 적어 뒀습니다:
- **Spirit Bloom** — 2.5x 로 표기한 출처와 3.0x 로 표기한 출처가 있음
- **Volcano 요구 스피드** — 70만~100만으로 출처마다 다름
- **Cosmic 등급** — 별도 티어로 세는 출처와 세지 않는 출처가 있음

현재 게임에는 **코드 리딤 시스템 자체가 없습니다.** "코드 나눠준다"는 글은 대부분 사기입니다.

<br>

## 구조

```
src/eggmate/
  core/               GUI 없는 순수 계산 로직 (Qt 를 임포트하지 않음 — 테스트로 고정)
    income.py           무게/뮤테이션 수입 공식
    odds.py             확률, 이항분포, Wilson 신뢰구간
    simulate.py         몬테카를로 (기하분포 역변환, 히스토그램)
    collection.py       쿠폰 수집가 (수치적분), 도감 진행률
    pen.py              펜 최적화, 인벤토리 변환, 슬롯 가치 곡선
    growth.py           업그레이드 큐, 재투자 곡선, 훈련 시간, 런 효율
    planner.py          바이옴 판정, ROI, 목표 시간
    fuse.py             퓨즈 손익
    storage.py          SQLite — 부화/인벤토리/위시/타임라인 + 프로필 + 백업
    settings.py         설정 (테마·글자크기·즐겨찾기·창 상태)
    dataset.py          gamedata.json 로딩 + 사용자 오버라이드
    diagnostics.py      표준출력 인코딩 보정, --selftest
    fmt.py              K/M/B 및 만/억/조 표기
  ui/                 PySide6 화면
    app.py              사이드바 + 스택 메인 윈도우
    context.py          데이터셋·DB·설정 공유 + 화면 간 변경 신호
    search.py           전역 검색 (Ctrl+K)
    compact.py          컴팩트 오버레이 (Ctrl+M)
    theme.py            다크/라이트 팔레트 + 글자 배율
    widgets.py          공용 위젯, 카드, 의존성 없는 막대 그래프
    tabs/               화면 14개
  data/
    gamedata.json       출처가 붙은 게임 수치
tests/                단위 테스트 300개
packaging/            아이콘 생성, PyInstaller 스펙, Inno Setup, CI 검증 스크립트
```

계산 로직은 UI 와 완전히 분리돼 있어서 GUI 없이도 import 해 쓸 수 있습니다.

```bash
python -m pytest tests/ -q      # 300 passed
```

<br>

## 라이선스

MIT. 자세한 내용은 [LICENSE](LICENSE).

이 프로젝트는 팬메이드이며 Roblox Corporation 이나 게임 개발자와 아무 관련이 없습니다.
